accelerate==1.0.1
diffusers==0.38.0
einops==0.8.1
fastapi==0.133.0
huggingface-hub==1.27.0
# insightface==1.0.1 is installed separately with --no-deps (see the
# Dockerfile): its declared opencv-python dependency conflicts with the
# headless OpenCV build pinned below.
numpy==1.26.4
# insightface imports the onnx module itself at import time (in addition to
# onnxruntime for inference), so the onnx package stays pinned here.
onnx==1.17.0
onnxruntime==1.28.0
opencv-python-headless==4.10.0.84
peft==0.20.0
pillow==12.3.0
python-multipart==0.0.31
requests==2.32.5
safetensors==0.8.0
scikit-image==0.26.0
scipy==1.14.1
setuptools==78.1.1
starlette==1.3.1
tokenizers==0.22.2
tqdm==4.67.1
transformers==5.5.0
uvicorn[standard]==0.36.0
